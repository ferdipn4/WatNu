# @vercel/analytics lists optional peers (SvelteKit, Vue, Remix) whose own peers clash with vitest's vite;
# none of them is used here, so npm must not try to satisfy them. Keeps npm ci in CI identical to local installs.
legacy-peer-deps=true

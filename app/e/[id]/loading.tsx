// app/e/[id]/loading.tsx — the event detail's shape while the event (and its organizer) is read.
import { SkeletonDetail } from "@/app/_components/Skeleton";

export default function Loading() {
  return <SkeletonDetail />;
}

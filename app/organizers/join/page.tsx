// app/organizers/join/page.tsx — "/organizers/join": an organizer that is not on WatNu yet asks
// for access. Linked from the directory, the sign-in screen and the profile tab.
import { JoinScreen } from "./_components/JoinScreen";

export default function JoinPage() {
  return <JoinScreen />;
}

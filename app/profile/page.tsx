// app/profile/page.tsx — "/profile", the fourth tab: your name, theme and language — or, signed in as
// an organizer, your organization's shortcuts and settings.
import { ProfileScreen } from "./_components/ProfileScreen";

export default function ProfilePage() {
  return <ProfileScreen />;
}

# Create event · steps 3–4

Step 3 is the AI-filled form: a maas "Translated from Dutch" note, then Fields 12px apart — Title, Date + Start, End + Price, Location, Address, Category (`select`, "AI guess"), the Newcomers welcome switch, Description — with the fields the AI could not read in the amber `missing` state, and a sticky footer button "Check & continue". Step 4 shows the EventCard as it will publish, then the three WarningPanels that apply (conflict, duplicate with its two answers, suggestion with "Move to Wednesday") and a sticky "Publish" that is never disabled by them.

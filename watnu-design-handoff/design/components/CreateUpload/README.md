# Create event · steps 1–2

Step 1 is one decision: the dashed upload Card ("Upload screenshot or poster") or the secondary "Paste text instead" under an `or` divider, with a step indicator under the top bar. Step 2 shows the uploaded poster in a floating frame with the accent scan line, "Reading your poster…", and a Card of `.wn-check` rows that fill in as the AI finds the title, date and time, place and price — the current step spins in accent. It advances on its own.

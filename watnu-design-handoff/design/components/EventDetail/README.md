# Event detail

The organizer's own image as the hero (`radius-xl`, on-image round back, share and bookmark buttons), the tags row, the `title`, three fact rows (when, where with address and walking distance, price), the description, the tappable organizer Card, Save + Add to calendar, and — when the organizer set a promo — the accent promo Card "10% off with WatNu". The second phone is the sheet it opens over the `scrim`: the QR at 220px on a white tile (in dark too), the code in the display face, and Done.

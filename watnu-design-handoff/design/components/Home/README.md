# Home · This week

The week grouped by day — Today, Tomorrow, then weekday names — filtered by one chip row; shown here in light and dark from the same file. Header (wordmark, `display` title, one `meta` line), the chip row bleeding to the edges, then `.wn-day` headers over `.wn-list`s of EventCards, and the TabBar with `week` active. See the Screens section for the filter rules and the empty case.

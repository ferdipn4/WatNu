# Organizers

The searchable directory (search Field, type chips, OrganizerCards with Follow) and an organizer profile: OrgLogo `lg`, name, type · category, the Instagram handle as a link, a full-width Follow, the description, the maas stats Card the organizer alone sees ("Promo codes redeemed · Sep", the `stat` number, the delta), then their upcoming EventCards. The profile has a top bar with back and share instead of the TabBar.
